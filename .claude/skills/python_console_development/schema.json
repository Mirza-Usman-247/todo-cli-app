{
  "type": "object",
  "properties": {
    "spec_file": {
      "type": "string",
      "description": "Path to spec file describing the console application requirements"
    },
    "features": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of features to implement, such as 'add_task', 'view_tasks', 'update_task', 'delete_task', 'search_tasks', 'export_import'"
    }
  },
  "required": ["spec_file"],
  "strict": true
}
